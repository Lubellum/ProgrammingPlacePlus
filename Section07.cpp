#include <iostream>

void Section07()
{
	// 問題①　次の各変換は、static_cast、const_cast、reinterpret_cast、Ｃ言語形式のキャスト、キャストは不要、のいずれで行えるか答えてください。

	// long → short
	// bool → int
	// int* → int
	// const char* → char*
	// struct MyData* → void*
	// struct MyData* → const struct MyData*
	// void* → struct MyData*
	// const float* → int*
	// bool (*f)(int) → bool (*f)(const char*)
}